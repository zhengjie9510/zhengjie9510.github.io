# 项目环境与工具偏好

## 命令执行优先原则

- **优先使用系统命令（Shell）** 完成文件操作、文本处理、环境检查等任务，避免不必要的脚本封装。

## Python 环境隔离原则

- **严禁在系统 Python 环境下安装任何依赖**。
- 所有 Python 包必须安装到 **当前项目目录下的虚拟环境**（`.venv/`）中。
- 执行 Python 脚本或安装依赖前，先确认已激活 `.venv` 虚拟环境；若未激活，使用 `.venv/bin/python` 或 `.venv/bin/pip` 的完整路径调用。
